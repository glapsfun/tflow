---
name: pass-trigger-condition
description: Use when an observable trigger condition applies to the current state
---

This fixture locks in the WR-02 fix: "trigger" is no longer a banned verb.

portability-notes.md documents this exact trigger-condition phrasing as the
recommended description style, so the validator must accept it (exit 0) rather
than reject the wording it teaches.
