---
name: something-else
description: Use when you need to test name-to-directory mismatch detection
---

This fixture has a frontmatter name that does not match the directory name.

The directory is named "fail-name-mismatch" but the frontmatter says
name: something-else — this violates R-NAME-DIR (spec).

Only one rule is broken: R-NAME-DIR (spec).
