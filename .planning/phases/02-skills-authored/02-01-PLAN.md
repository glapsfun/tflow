---
phase: 02-skills-authored
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skill-creator/
  - skills/tflow-skill-creator/scripts/validate.sh
  - skills/tflow-skill-creator/scripts/run-tests.sh
  - skills/tflow-skill-creator/scripts/init.sh
  - skills/tflow-skill-creator/scripts/improve.sh
  - skills/tflow-skill-creator/scripts/package.sh
  - skills/tflow-skill-creator/scripts/fixtures/
  - skills/tflow-skill-creator/assets/scaffold/SKILL.md
autonomous: true
requirements:
  - CREATE-05
  - CREATE-06
  - CREATE-07
  - PORT-01
  - PORT-02
must_haves:
  truths:
    - "Running sh skills/tflow-skill-creator/scripts/init.sh <name> creates skills/<name> with SKILL.md, scripts/, references/, and assets/ and validates the result per D-01, D-02, D-03, and D-04."
    - "Running sh skills/tflow-skill-creator/scripts/improve.sh <skill-dir-or-md> writes .skill-improvement.md with validation result, scaffold comparison, placeholder checks, and a human testing checklist per D-09 and D-10."
    - "Running sh skills/tflow-skill-creator/scripts/package.sh <skill-dir> validates first, requires completed .skill-improvement.md, creates dist/<skill-name>/ and dist/<skill-name>.tar.gz, and never installs into runtime paths per D-05, D-06, D-07, D-08, and D-11."
  artifacts:
    - path: "skills/tflow-skill-creator/scripts/validate.sh"
      provides: "Carried-forward Phase 1 validation gate"
    - path: "skills/tflow-skill-creator/scripts/init.sh"
      provides: "Strict skill scaffolding command"
    - path: "skills/tflow-skill-creator/scripts/improve.sh"
      provides: "Improvement evidence report command"
    - path: "skills/tflow-skill-creator/scripts/package.sh"
      provides: "Validation-gated packaging command"
    - path: "skills/tflow-skill-creator/scripts/run-tests.sh"
      provides: "POSIX sh self-test runner"
    - path: "skills/tflow-skill-creator/assets/scaffold/SKILL.md"
      provides: "Validated starter SKILL.md scaffold"
  key_links:
    - from: "skills/tflow-skill-creator/scripts/init.sh"
      to: "skills/tflow-skill-creator/scripts/validate.sh"
      via: "post-scaffold validation"
      pattern: "validate\\.sh"
    - from: "skills/tflow-skill-creator/scripts/package.sh"
      to: ".skill-improvement.md"
      via: "completed checklist gate"
      pattern: "\\.skill-improvement\\.md"
    - from: "skills/tflow-skill-creator/scripts/run-tests.sh"
      to: "skills/tflow-skill-creator/scripts/fixtures/"
      via: "fixture exit-code assertions"
      pattern: "fixtures"
---

<objective>
Migrate the Phase 1 script spine to the canonical `tflow-skill-creator` source tree and implement the three remaining deterministic POSIX `sh` commands: `init.sh`, `improve.sh`, and `package.sh`.

Purpose: This is the shared execution foundation for the authored skill factory. It preserves the stable Phase 1 `validate.sh` contract while adding the Phase 2 script gates required by CREATE-05, CREATE-06, CREATE-07, PORT-01, and PORT-02.
Output: Canonical script tree under `skills/tflow-skill-creator/`, scaffold asset, expanded fixture suite, and self-tests.
</objective>

<execution_context>
@/Users/vladtara/dev/github/glapsfun/tflow/.codex/gsd-core/workflows/execute-plan.md
@/Users/vladtara/dev/github/glapsfun/tflow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-skills-authored/02-CONTEXT.md
@.planning/phases/02-skills-authored/02-RESEARCH.md
@.planning/phases/02-skills-authored/02-PATTERNS.md
@.planning/phases/01-the-keystone-gate/01-01-SUMMARY.md
@skills/skill-creator/scripts/validate.sh
@skills/skill-creator/scripts/run-tests.sh
</context>

## Artifacts This Phase Produces

- Skill directories: `skills/tflow-skill-creator/`, `skills/tflow-research/`
- Scripts: `validate.sh`, `run-tests.sh`, `init.sh`, `improve.sh`, `package.sh`
- CLI flags/contracts: `validate.sh [--quiet|-q] [<skill-dir>]`, `init.sh <skill-name> [target-root]`, `improve.sh <skill-dir-or-skill-md>`, `package.sh <skill-dir>`
- Reference files: `factory-loop.md`, `portability-notes.md`, `testing-checklist.md`, `research-loop.md`, `source-confidence.md`, `brief-schema.md`
- Assets: `skills/tflow-skill-creator/assets/scaffold/SKILL.md`
- Generated marker/report files: `.skill-improvement.md`
- Package artifacts: `dist/<skill-name>/`, `dist/<skill-name>.tar.gz`
- Phase summaries: `.planning/phases/02-skills-authored/02-01-SUMMARY.md`, `02-02-SUMMARY.md`, `02-03-SUMMARY.md`

<tasks>

<task type="auto">
  <name>Task 1: Migrate the canonical script spine</name>
  <files>skills/skill-creator/, skills/tflow-skill-creator/scripts/validate.sh, skills/tflow-skill-creator/scripts/run-tests.sh, skills/tflow-skill-creator/scripts/fixtures/</files>
  <read_first>
    - .planning/phases/02-skills-authored/02-CONTEXT.md
    - .planning/phases/02-skills-authored/02-RESEARCH.md
    - .planning/phases/01-the-keystone-gate/01-01-SUMMARY.md
    - skills/skill-creator/scripts/validate.sh
    - skills/skill-creator/scripts/run-tests.sh
  </read_first>
  <action>Use `git mv` where possible to migrate `skills/skill-creator/scripts/validate.sh`, `run-tests.sh`, and all existing fixtures into `skills/tflow-skill-creator/scripts/` so the canonical source name implements D-18 without losing Phase 1 artifacts. Update script comments and self-test paths only where the old path is embedded in executable behavior. Do not change the stable `validate.sh [--quiet|-q] [<skill-dir>]` CLI contract from Phase 1. Do not create runtime installs in `.claude/`, `.codex/`, or `.agents/`.</action>
  <verify>
    <automated>test -f skills/tflow-skill-creator/scripts/validate.sh &amp;&amp; test -f skills/tflow-skill-creator/scripts/run-tests.sh &amp;&amp; test -d skills/tflow-skill-creator/scripts/fixtures/pass-well-formed &amp;&amp; sh skills/tflow-skill-creator/scripts/run-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `skills/tflow-skill-creator/scripts/validate.sh`, `run-tests.sh`, and every Phase 1 fixture directory exist under the new canonical path.
    - Behavior assertion: `sh skills/tflow-skill-creator/scripts/run-tests.sh` still reports the Phase 1 fixture suite with zero failures.
    - Source assertion: no executable script still calls `skills/skill-creator/scripts/validate.sh`.
  </acceptance_criteria>
  <done>The Phase 1 validation gate and fixtures are preserved under `skills/tflow-skill-creator/scripts/` and the old roadmap labels remain historical only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scaffold, improvement, and package commands</name>
  <files>skills/tflow-skill-creator/scripts/init.sh, skills/tflow-skill-creator/scripts/improve.sh, skills/tflow-skill-creator/scripts/package.sh, skills/tflow-skill-creator/assets/scaffold/SKILL.md</files>
  <read_first>
    - .planning/phases/02-skills-authored/02-CONTEXT.md
    - .planning/phases/02-skills-authored/02-RESEARCH.md
    - .planning/phases/02-skills-authored/02-PATTERNS.md
    - skills/tflow-skill-creator/scripts/validate.sh
    - skills/tflow-skill-creator/scripts/run-tests.sh
  </read_first>
  <behavior>
    - Test 1: `init.sh valid-skill <tmp-root>` creates `<tmp-root>/skills/valid-skill/{SKILL.md,scripts,references,assets}` and the generated skill passes `validate.sh`.
    - Test 2: `init.sh InvalidName <tmp-root>` exits non-zero with a clear invalid-name error per D-02.
    - Test 3: `init.sh existing-skill <tmp-root>` exits non-zero when the target directory already exists per D-03.
    - Test 4: `improve.sh <skill-dir>` writes `<skill-dir>/.skill-improvement.md` with validation status, scaffold comparison where practical, placeholder checks, and a testing checklist per D-09 and D-10.
    - Test 5: `package.sh <skill-dir>` exits non-zero when validation fails or `.skill-improvement.md` contains unchecked checklist items per D-08 and D-11.
    - Test 6: `package.sh <skill-dir>` creates `dist/<skill-name>/` and `dist/<skill-name>.tar.gz` only after validation and completed improvement evidence per D-05, D-06, and D-07.
  </behavior>
  <action>Create POSIX `sh` implementations with `set -eu`, script-relative path resolution, quoted expansions, usage errors as exit 2, and operational failures as exit 1. `init.sh` must enforce the same lowercase hyphenated name rule as `validate.sh`, refuse existing targets, create the required support directories, write a neutral guided starter `SKILL.md` from `assets/scaffold/SKILL.md`, and run sibling `validate.sh` on the generated skill per D-01 through D-04. `improve.sh` must accept a skill directory or `SKILL.md` path, run validation, compare the current skill to the scaffold where practical, check obvious starter leftovers, and write `.skill-improvement.md` without rewriting source per D-09 and D-10. `package.sh` must validate and check `.skill-improvement.md` before touching `dist/`, stage an inspectable copy excluding old `dist/`, caches, temp/checkpoint files, and transient generated artifacts, then create the archive and print copy/install hints without performing installation per D-05 through D-08 and D-11.</action>
  <verify>
    <automated>sh -n skills/tflow-skill-creator/scripts/init.sh &amp;&amp; sh -n skills/tflow-skill-creator/scripts/improve.sh &amp;&amp; sh -n skills/tflow-skill-creator/scripts/package.sh &amp;&amp; sh skills/tflow-skill-creator/scripts/run-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all three new scripts use `#!/bin/sh` or are invoked cleanly by `sh`, use `set -eu`, and contain no bash-only `[[`, arrays, or `local` declarations.
    - Behavior assertion: generated scaffold skills validate cleanly immediately after `init.sh`.
    - Behavior assertion: failed validation prevents package artifact writes or updates.
    - Behavior assertion: completed improvement evidence enables `package.sh` to create both inspectable directory and archive artifacts.
  </acceptance_criteria>
  <done>`init.sh`, `improve.sh`, and `package.sh` satisfy CREATE-05, CREATE-06, CREATE-07 and the D-01 through D-11 script contracts with automated self-test coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the POSIX self-test runner</name>
  <files>skills/tflow-skill-creator/scripts/run-tests.sh, skills/tflow-skill-creator/scripts/fixtures/</files>
  <read_first>
    - skills/tflow-skill-creator/scripts/run-tests.sh
    - skills/tflow-skill-creator/scripts/validate.sh
    - skills/tflow-skill-creator/scripts/init.sh
    - skills/tflow-skill-creator/scripts/improve.sh
    - skills/tflow-skill-creator/scripts/package.sh
    - .planning/phases/02-skills-authored/02-PATTERNS.md
  </read_first>
  <action>Extend `run-tests.sh` while preserving the pass-/fail-fixture convention and POSIX `sh` style. Add isolated temporary-directory tests for scaffold shape, invalid names, no-overwrite behavior, improvement report generation, package refusal with unchecked evidence, and successful package artifacts. Keep tests deterministic and clean temporary files with a trap. Do not add bats, Python, Node, or package-manager dependencies.</action>
  <verify>
    <automated>sh skills/tflow-skill-creator/scripts/run-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: the test runner exits 0 only when validate, init, improve, and package behavior assertions all pass.
    - Source assertion: test runner keeps the existing fixture loop and adds script-specific tests without relying on runtime install paths.
    - Behavior assertion: test output includes zero failed tests and names the script contract that failed when a regression is introduced.
  </acceptance_criteria>
  <done>The canonical script spine is self-tested end to end using only portable shell tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to local scripts | Untrusted skill names and paths influence filesystem writes. |
| Skill source to package output | Local source is copied into distributable artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `init.sh` name and target path | mitigate | Enforce lowercase hyphenated skill names, reject slash/dot/empty names, and refuse existing target directories per D-02 and D-03. |
| T-02-02 | Tampering | `package.sh` artifact writes | mitigate | Run validation and improvement-evidence gates before writing `dist/`; use temp staging and only replace artifacts after all checks pass per D-08. |
| T-02-03 | Elevation | shell argument handling | mitigate | Use POSIX `sh`, quoted expansions, no eval, `sh -n`, and `validate.sh` shellcheck branch when available. |
| T-02-SC | Tampering | package installs | accept | No npm, pip, cargo, or external package installs are part of this phase; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Run `sh skills/tflow-skill-creator/scripts/run-tests.sh` after all tasks. If `shellcheck` is available, `sh skills/tflow-skill-creator/scripts/validate.sh skills/tflow-skill-creator` must also run the shellcheck branch cleanly; if unavailable, the existing visible WARN path is acceptable.
</verification>

<success_criteria>
- `skills/tflow-skill-creator/scripts/validate.sh`, `run-tests.sh`, fixtures, `init.sh`, `improve.sh`, `package.sh`, and `assets/scaffold/SKILL.md` exist under the canonical renamed skill path.
- Script commands satisfy D-01 through D-11 and requirements CREATE-05, CREATE-06, CREATE-07, PORT-01, and PORT-02.
- Self-tests pass with zero failures.
</success_criteria>

<output>
Create `.planning/phases/02-skills-authored/02-01-SUMMARY.md` when done.
</output>
